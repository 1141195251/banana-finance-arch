<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.ledger.dao.BankDepositDiaryDao">

    <resultMap type="com.jluzh.ledger.entity.BankDepositDiary" id="BankDepositDiaryMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="voucherId" column="voucher_id" jdbcType="VARCHAR"/>
        <result property="summary" column="summary" jdbcType="VARCHAR"/>
        <result property="toAccountId" column="to_account_id" jdbcType="INTEGER"/>
        <result property="income" column="income" jdbcType="NUMERIC"/>
        <result property="expense" column="expense" jdbcType="NUMERIC"/>
        <result property="balance" column="balance" jdbcType="NUMERIC"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.bank_deposit_diary(create_time, voucher_id, summary, to_account_id, income, expense,
        balance)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createTime}, #{entity.voucherId}, #{entity.summary}, #{entity.toAccountId}, #{entity.income},
            #{entity.expense}, #{entity.balance})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.bank_deposit_diary(create_time, voucher_id, summary, to_account_id, income, expense,
        balance)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createTime}, #{entity.voucherId}, #{entity.summary}, #{entity.toAccountId}, #{entity.income},
            #{entity.expense}, #{entity.balance})
        </foreach>
        on duplicate key update
        create_time = values(create_time) , voucher_id = values(voucher_id) , summary = values(summary) , to_account_id
        = values(to_account_id) , income = values(income) , expense = values(expense) , balance = values(balance)
    </insert>

</mapper>

