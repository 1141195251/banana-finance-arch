<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.ledger.dao.DtMaterialAccountDao">

    <resultMap type="com.jluzh.ledger.entity.DtMaterialAccount" id="DtMaterialAccountMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="voucherId" column="voucher_id" jdbcType="VARCHAR"/>
        <result property="summary" column="summary" jdbcType="VARCHAR"/>
        <result property="inCount" column="in_count" jdbcType="INTEGER"/>
        <result property="inUnitPrice" column="in_unit_price" jdbcType="NUMERIC"/>
        <result property="inPrice" column="in_price" jdbcType="NUMERIC"/>
        <result property="outCount" column="out_count" jdbcType="INTEGER"/>
        <result property="outUprice" column="out_uprice" jdbcType="NUMERIC"/>
        <result property="outPrice" column="out_price" jdbcType="NUMERIC"/>
        <result property="balanceCount" column="balance_count" jdbcType="INTEGER"/>
        <result property="balanceUprice" column="balance_uprice" jdbcType="NUMERIC"/>
        <result property="balance" column="balance" jdbcType="NUMERIC"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.dt_material_account(create_time, voucher_id, summary, in_count, in_unit_price, in_price,
        out_count, out_uprice, out_price, balance_count, balance_uprice, balance)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createTime}, #{entity.voucherId}, #{entity.summary}, #{entity.inCount}, #{entity.inUnitPrice},
            #{entity.inPrice}, #{entity.outCount}, #{entity.outUprice}, #{entity.outPrice}, #{entity.balanceCount},
            #{entity.balanceUprice}, #{entity.balance})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.dt_material_account(create_time, voucher_id, summary, in_count, in_unit_price, in_price,
        out_count, out_uprice, out_price, balance_count, balance_uprice, balance)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.createTime}, #{entity.voucherId}, #{entity.summary}, #{entity.inCount}, #{entity.inUnitPrice},
            #{entity.inPrice}, #{entity.outCount}, #{entity.outUprice}, #{entity.outPrice}, #{entity.balanceCount},
            #{entity.balanceUprice}, #{entity.balance})
        </foreach>
        on duplicate key update
        create_time = values(create_time) , voucher_id = values(voucher_id) , summary = values(summary) , in_count =
        values(in_count) , in_unit_price = values(in_unit_price) , in_price = values(in_price) , out_count =
        values(out_count) , out_uprice = values(out_uprice) , out_price = values(out_price) , balance_count =
        values(balance_count) , balance_uprice = values(balance_uprice) , balance = values(balance)
    </insert>

</mapper>

