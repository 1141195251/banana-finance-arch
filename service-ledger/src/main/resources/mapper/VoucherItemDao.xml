<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.ledger.dao.VoucherItemDao">

    <resultMap type="com.jluzh.ledger.entity.VoucherItem" id="VoucherItemMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="applicant" column="applicant" jdbcType="INTEGER"/>
        <result property="deptId" column="dept_id" jdbcType="INTEGER"/>
        <result property="appDate" column="app_date" jdbcType="TIMESTAMP"/>
        <result property="total" column="total" jdbcType="NUMERIC"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="comment" column="comment" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.voucher_item(title, applicant, dept_id, app_date, total, status, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.applicant}, #{entity.deptId}, #{entity.appDate}, #{entity.total},
            #{entity.status}, #{entity.comment})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.voucher_item(title, applicant, dept_id, app_date, total, status, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.applicant}, #{entity.deptId}, #{entity.appDate}, #{entity.total},
            #{entity.status}, #{entity.comment})
        </foreach>
        on duplicate key update
        title = values(title) , applicant = values(applicant) , dept_id = values(dept_id) , app_date = values(app_date)
        , total = values(total) , status = values(status) , comment = values(comment)
    </insert>

</mapper>

