<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.reimbursement.dao.TbReimbursementInfoDao">

    <resultMap type="com.jluzh.reimbursement.entity.TbReimbursementInfo" id="TbReimbursementInfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="cause" column="cause" jdbcType="VARCHAR"/>
        <result property="createSn" column="create_sn" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="nextDealSn" column="next_deal_sn" jdbcType="VARCHAR"/>
        <result property="totalAmount" column="total_amount" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_info(cause, create_sn, create_time, next_deal_sn, total_amount, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.cause}, #{entity.createSn}, #{entity.createTime}, #{entity.nextDealSn}, #{entity.totalAmount},
            #{entity.status})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_info(cause, create_sn, create_time, next_deal_sn, total_amount, status)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.cause}, #{entity.createSn}, #{entity.createTime}, #{entity.nextDealSn}, #{entity.totalAmount},
            #{entity.status})
        </foreach>
        on duplicate key update
        cause = values(cause) , create_sn = values(create_sn) , create_time = values(create_time) , next_deal_sn =
        values(next_deal_sn) , total_amount = values(total_amount) , status = values(status)
    </insert>

</mapper>

