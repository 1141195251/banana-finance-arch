<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.reimbursement.dao.TbReimbursementRecordDao">

    <resultMap type="com.jluzh.reimbursement.entity.TbReimbursementRecord" id="TbReimbursementRecordMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="claimVoucherId" column="claim_voucher_id" jdbcType="INTEGER"/>
        <result property="dealSn" column="deal_sn" jdbcType="VARCHAR"/>
        <result property="dealTime" column="deal_time" jdbcType="TIMESTAMP"/>
        <result property="dealWay" column="deal_way" jdbcType="VARCHAR"/>
        <result property="dealResult" column="deal_result" jdbcType="VARCHAR"/>
        <result property="comment" column="comment" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_record(claim_voucher_id, deal_sn, deal_time, deal_way, deal_result, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.claimVoucherId}, #{entity.dealSn}, #{entity.dealTime}, #{entity.dealWay}, #{entity.dealResult}, #{entity.comment})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_record(claim_voucher_id, deal_sn, deal_time, deal_way, deal_result, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.claimVoucherId}, #{entity.dealSn}, #{entity.dealTime}, #{entity.dealWay}, #{entity.dealResult}, #{entity.comment})
        </foreach>
        on duplicate key update
         claim_voucher_id = values(claim_voucher_id) , deal_sn = values(deal_sn) , deal_time = values(deal_time) , deal_way = values(deal_way) , deal_result = values(deal_result) , comment = values(comment)     </insert>

</mapper>

