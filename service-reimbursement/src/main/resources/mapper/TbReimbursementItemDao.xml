<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.reimbursement.dao.TbReimbursementItemDao">

    <resultMap type="com.jluzh.reimbursement.entity.TbReimbursementItem" id="TbReimbursementItemMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="claimVoucherId" column="claim_voucher_id" jdbcType="INTEGER"/>
        <result property="item" column="item" jdbcType="VARCHAR"/>
        <result property="amount" column="amount" jdbcType="VARCHAR"/>
        <result property="comment" column="comment" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_item(claim_voucher_id, item, amount, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.claimVoucherId}, #{entity.item}, #{entity.amount}, #{entity.comment})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.tb_reimbursement_item(claim_voucher_id, item, amount, comment)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.claimVoucherId}, #{entity.item}, #{entity.amount}, #{entity.comment})
        </foreach>
        on duplicate key update
         claim_voucher_id = values(claim_voucher_id) , item = values(item) , amount = values(amount) , comment = values(comment)     </insert>

</mapper>

