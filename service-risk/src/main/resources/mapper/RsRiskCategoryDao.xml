<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jluzh.risk.dao.RsRiskCategoryDao">

    <resultMap type="com.jluzh.risk.entity.RsRiskCategory" id="RsRiskCategoryMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="riskCategoryName" column="risk_category_name" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.rs_risk_category(risk_category_name, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.riskCategoryName}, #{entity.createTime})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bfinance.rs_risk_category(risk_category_name, create_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.riskCategoryName}, #{entity.createTime})
        </foreach>
        on duplicate key update
        risk_category_name = values(risk_category_name) , create_time = values(create_time)
    </insert>

</mapper>

